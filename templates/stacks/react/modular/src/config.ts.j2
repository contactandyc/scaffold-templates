{#- scaffold-repo: { dest: "src/config.ts" } -#}
// Point this to your Express backend
export const API_BASE_URL = 'http://localhost:{{ api_port | default("8080") }}/api';
{% if websockets in ['yes', true, 'true', 'True'] %}
export const WS_BASE_URL = 'http://localhost:{{ api_port | default("8080") }}';
{% endif %}
