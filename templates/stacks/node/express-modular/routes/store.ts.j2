{#- scaffold-repo: { dest: "src/routes/store.{{ 'ts' if use_typescript == 'yes' else 'js' }}" } -#}
{% if kv_backend != 'None' %}
// SPDX-FileCopyrightText: {{ year | default('2026') }} {{ author | default('Andy Curtis <contactandyc@gmail.com>') }}
// SPDX-License-Identifier: Apache-2.0
//
// Maintainer: {{ author | default('Andy Curtis <contactandyc@gmail.com>') }}

import { Router } from 'express';
import { createOrUpdate, retrieve, remove } from '../controllers/store';

const router = Router();

router.post('/', createOrUpdate);
router.get('/:key', retrieve);
router.delete('/:key', remove);

export default router;
{% endif %}
