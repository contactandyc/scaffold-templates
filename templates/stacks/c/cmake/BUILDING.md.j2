{#- scaffold-repo: { dest: "BUILDING.md" } -#}
# BUILDING

This project: **{{ project_title }}**
Version: **{{ version }}**

## Local build

```bash
# one-shot build + install
./build.sh install
```

Or run the steps manually:

```bash
mkdir -p build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . -j"$(nproc || sysctl -n hw.ncpu || echo 4)"
sudo cmake --install .
```

{% set libs = deps.get('libraries', []) if deps is mapping else [] %}
{% set apt_pkgs = deps.get('apt_packages', []) if deps is mapping else [] %}
{% set apt_dev = deps.get('apt_dev_packages', []) if deps is mapping else [] %}

{% if libs %}

## Install dependencies (from `deps.libraries`)

{% if apt_pkgs and apt_pkgs|length %}

### System packages (required)

```bash
sudo apt-get update && sudo apt-get install -y {{ apt_pkgs|join(' ') }}
```

{% endif %}

{% if apt_dev and apt_dev|length %}

### Development tooling (optional)

```bash
sudo apt-get update && sudo apt-get install -y {{ apt_dev|join(' ') }}
```

{% endif %}

{% for lib in libs %}

### {{ lib.get('name', 'dependency') }}

{% if lib.get('build_steps') %}
Clone & build:

{% set lib_url = lib.get('url') or ("https://github.com/" ~ github_project|default('contactandyc') ~ "/" ~ lib.name ~ ".git") %}
{% set clean_steps = [] %}
{% for s in lib.get('build_steps', []) %}
  {% if 'git clone' not in s and 'rm -rf' not in s and not s.startswith('cd ') %}
    {% set _ = clean_steps.append(s) %}
  {% endif %}
{% endfor %}
```bash
git clone {% if lib.get('shallow', true) %}--depth 1 {% endif %}{% if lib.get('branch') %}--branch {{ lib.get('branch') }} {% endif %}--single-branch "{{ lib_url }}" "{{ lib.name }}"
cd "{{ lib.name }}"
{% for s in clean_steps %}
{{ s }}
{% endfor %}
cd ..
rm -rf "{{ lib.name }}"
```

{% elif lib.get('kind') == "system" %}
Install via package manager:

```bash
sudo apt-get update && sudo apt-get install -y {{ lib.get('pkg', '...') }}
```

{% endif %}
{% endfor %}
{% endif %}
