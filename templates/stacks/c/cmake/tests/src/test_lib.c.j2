{#- scaffold-repo: { dest: "tests/src/test_{{ project_snake }}.c", updatable: false } -#}
#include "{{ project_slug }}/{{ project_snake }}.h"
#include <stdio.h>

int main(void) {
    {{ project_snake }}_hello();
    printf("{{ project_name }} test passed!\n");
    return 0;
}
